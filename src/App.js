import './App.css'
import NavBar from './componentes/navbar';
import botones from './componentes/botones/botones';
import card from './componentes/card/card';






function App() {
    const categorias = ["inicio", "pokemon", "tipos"]
    return ( < > < NavBar / > < div className = "App" / >= {
            "Bienvenidos a la app!"
        }
        /> </ > );
}

export default App;